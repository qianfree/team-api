# Team-API 代码分析结果

> **生成时间**：2026-07-25
> **分析范围**：约 6.9 万行手写业务代码
> **已排除**：脚手架生成代码（`controller/` `service/` `model/do/` `model/entity/` `dao/`）、`web/` 前端、`relay/` 顶级代理目录、测试代码
> **覆盖模块**：`api/` `internal/logic/` `internal/cmd/` `internal/consts/` `internal/handler/` `internal/response/` `internal/middleware/` `internal/utility/`
> **分析方法**：4 个并行子 agent 分模块深度审查 + 人工 spot-check 逐行验证所有 P0 结论

---


## 🟠 P1 — 严重问题（建议本迭代修复）

### 🔐 P1-Security　安全类

#### - [ ] P1-S1　RBAC 未匹配路由默认放行

- **位置**：`internal/middleware/rbac.go:332-337`
- **问题描述**：
  ```go
  perm := matchPermission(r.Method, r.URL.Path)
  if perm == "" {
      // 默认放行：未配置权限规则的接口，对已认证 admin 放行
      r.Middleware.Next()
      return
  }
  ```
  注释自承是过渡策略。新增 admin 接口忘配规则 = 默认对所有已认证 admin 开放。
- **影响**：越权风险取决于开发者纪律而非系统约束，是 RBAC 的根本性缺口。
- **修复建议**：
  1. 改为默认 403。
  2. 启动期扫描 `api/admin/v1/` 的路由注解，与 `adminPermissionRules` 做 diff，未覆盖的打 warning（最好接入 CI 检查）。
- **验证方法**：临时新增一个未配规则的 admin 接口，请求应返回 403。

#### - [ ] P1-S2　TOTP / OpenAPI nonce 防重放 fail-open

- **位置**：`internal/logic/common/security.go:39-42`、`internal/middleware/open_auth.go:145-159`
- **问题描述**：两处都在 Redis 故障时 `return true` 放行（注释写"避免依赖故障导致无法登录"）。
- **影响**：Redis 抖动期间同一 TOTP 码可无限重放（2FA 退化为单因素）；HMAC 签名请求可在时间窗内重放。
- **修复建议**：敏感操作（2FA、支付类 webhook）Redis 不可用时 **fail-closed**（拒绝）+ 触发告警；非敏感操作可放行但记指标。

#### - [ ] P1-S3　consumeBackupCode TOCTOU

- **位置**：`internal/logic/common/security.go:495-518`
- **问题描述**：先 `GetUserTOTPSecret` 读全部 backup codes → 内存删一个 → 整体写回，两步非原子。
- **影响**：两个并发请求用不同恢复码可同时读到完整列表并各自写回，导致其中一个恢复码被"复活"。
- **修复建议**：用单条 UPDATE 配合 JSONB 操作（`jsonb_remove` 或过滤数组），或在事务内 `SELECT FOR UPDATE`。


#### - [ ] P1-S5　OAuth 自动注册用户名冲突暴露技术细节

- **位置**：`internal/logic/tenant/oauth.go:236-257`
- **问题描述**：用户名冲突重试一次失败后，直接返回原始 DB error（含表名/约束名）。
- **影响**：违反 CLAUDE.md「message 必须中文，禁止暴露技术细节」。
- **修复建议**：捕获 `IsDuplicateKeyError` 返回业务错误「登录冲突，请重试」。


#### - [ ] P1-S7　generateState / randomHex 忽略 rand.Read 错误

- **位置**：`internal/logic/tenant/oauth.go:461-470`
- **问题描述**：`rand.Read(b)` 的 error 被 `_` 吞。系统熵池枯竭时返回全零 bytes，state 可预测。
- **修复建议**：检查 error，失败时返回 500。

---

### 💰 P1-Money　金额精度类（违反 CLAUDE.md 强约束）

#### - [ ] P1-M1　WalletInfo 全程 float64（billing 包最大精度破口）

- **位置**：`internal/logic/billing/wallet.go:41-49`
- **问题描述**：`Balance`/`FrozenBalance`/`WarningThreshold` 全是 float64。`GetWallet` 从 DB（NUMERIC/decimal）读出后立即降级为 float64 缓存。entity 已是 decimal，缓存层把它丢了。
- **影响**：全链路比较/运算在 float64 上，违反「金额必须 decimal」强约束；下游 `CheckBalanceWarning`、`AvailableBalance` 全受影响。
- **修复建议**：`WalletInfo` 字段改 `decimal.Decimal`，对外暴露 `AvailableBalance()` 返回 decimal，API Res 出口再 `.InexactFloat64()`。

#### - [ ] P1-M2　member_quota 用 HINCRBYFLOAT

- **位置**：`internal/logic/billing/member_quota.go:67`
- **问题描述**：Redis 用 `HINCRBYFLOAT` 累加 `quota_used`，正是规范明文要杜绝的反模式。
- **修复建议**：与 wallet 一致改整数 micro-USD（`billing.ToMicro`/`FromMicro` + `HINCRBY`），或文档化为"非资金控制线允许 float64"并加注释。

#### - [ ] P1-M3　API 入口金额字段大面积 float64

- **位置**：
  - `api/admin/v1/dashboard.go:260`（AdminWalletAdjustReq.Amount）
  - `api/admin/v1/tenant_level.go:14,35,50`（CumulativeRechargeThreshold）
  - `api/admin/v1/order_admin.go:25-27`
  - `api/admin/v1/promo.go:22,72`
  - `api/admin/v1/monitor.go:112,131`（Threshold）
  - `api/tenant/v1/order_tenant.go:20,52-54,95,107`
  - `api/tenant/v1/api_key.go`（TotalQuota）
- **问题描述**：入口 Req 金额字段直接用 float64 参与运算。
- **修复建议**：入口字段改 decimal，或入口立即 `billing.NewFromFloat(f)` 转进 decimal 再运算。

#### - [ ] P1-M4　promo_code 折扣用 float64 计算

- **位置**：`internal/logic/tenant/promo_code.go:133-153`
- **问题描述**：`amount * promo.DiscountValue / 100` 走 float64，且 `validatePromoCodeInternal` 返回 `map[string]any`。
- **修复建议**：改 decimal 运算；返回值改 `PromoValidationResult struct`。

#### - [ ] P1-M5　CheckBalanceWarning 用 float64 比较

- **位置**：`internal/logic/billing/alert.go:39,124`
- **问题描述**：`available`、`WarningThreshold` 均 float64，违反「金额比较用 .GreaterThan/.IsZero」。
- **修复建议**：依赖 P1-M1 的 WalletInfo decimal 化后自然解决。

---

### ⚡ P1-Concurrency　并发与一致性类

#### - [x] P1-C1　preDeductSyncDB 失败不回滚 Redis

- **位置**：`internal/logic/billing/wallet.go:230-237`
- **问题描述**：Lua 冻结成功后 DB UPDATE 失败**只记日志、不回滚 Redis**。
- **影响**：Redis `frozen_balance` 已增加但 DB 未变，后续预扣基于错误 Redis 值，租户余额被锁死直到 TTL（600s）或人工干预。
- **修复建议**：DB UPDATE 失败时 `InvalidateWalletRedis` 强制下次重建，并把 error 上抛。

#### - [x] P1-C2　结算事务失败不释放预扣冻结

- **位置**：`internal/logic/billing/settlement.go:243-256`
- **问题描述**：事务回滚后 frozen_balance 未释放、balance 未扣，调用方直接 `return err`，既不重试也不 `UnfreezePreDeduct`。
- **影响**：预扣冻结挂着，等 30 分钟 `CleanExpiredPreDeducts` 才释放，期间租户可用余额被锁。
- **修复建议**：事务失败（且非幂等冲突）后主动 `UnfreezePreDeduct` 释放冻结。

#### - [x] P1-C3　UnfreezePreDeduct 信任调用方传参

- **位置**：`internal/logic/billing/wallet.go:259-282`
- **问题描述**：解冻金额由调用方传入，DB 侧 `GREATEST(frozen - ?, 0)` 会"吞掉"超额解冻，掩盖 bug。
- **修复建议**：从 `prededuct:v2:{request_id}` hash 内读出冻结时的 amount（已存），不信任传参。

#### - [x] P1-C4　配额检查-递增非原子

- **位置**：`internal/logic/billing/api_key_quota.go:15-40`、`member_quota.go:35-58`
- **问题描述**：「读 used + 比较 preDeduct」与「结算后异步 Incr」分离。两个并发请求都通过检查 → 都结算 → 实际超限。
- **修复建议**：检查时同步原子预占（参考钱包预扣的 Lua），或在 Incr 时再次校验。

#### - [x] P1-C5　ExecuteTask 无 panic recover

- **位置**：`internal/logic/task/task.go:103-192`
- **问题描述**：`RunPendingTasks` 循环里调 `ExecuteTask` 没 recover。
- **影响**：一个任务 panic 会中断整个 cron 批次，后续任务全部丢失。
- **修复建议**：`ExecuteTask` 内部 `defer recover` 并把任务标为 failed。

#### - [x] P1-C6　monitor 包级共享状态无锁（data race）

- **位置**：`internal/logic/monitor/request_tracker.go:274-293`、`collector.go:328-342`
- **问题描述**：`lastBytesIn`/`lastBytesOut`/`lastBwTime`/`lastNetStats` 等包级字段无同步。`lastNetStats` 未首次赋值时 `lastNetStats[0]` 会 panic。
- **修复建议**：用 mutex 包裹或 `atomic.Pointer` 存上一次快照；确保首帧保护。

#### - [x] P1-C7　checkAutoRecover TOCTOU

- **位置**：`internal/logic/relay/health.go:223-258`
- **问题描述**：先 `Scan` 检查 `AutoDisabled == 1` 再无条件 `Update`，没有 `Where auto_disabled = 1` 谓词。
- **影响**：管理员手动禁用后仍可能被自动恢复逻辑翻成 active。
- **修复建议**：带谓词的条件更新 + 检查 RowsAffected。

#### - [x] P1-C8　refreshOAuthKey 无并发保护

- **位置**：`internal/logic/relay/provider.go:1106-1112`
- **问题描述**：两个并发请求同时发现 token 过期 → 同时 refresh → 同时 UPDATE。OAuth refresh token 通常一次性，第二次会失败。
- **修复建议**：`SELECT FOR UPDATE` 或 Redis 分布式锁串行化单 key 的刷新。

#### - [x] P1-C9　2FA backup code 遍历 bcrypt（DoS 风险）

- **位置**：`internal/logic/common/security.go:303-315`
- **问题描述**：每次 2FA 失败遍历最多 8 个 bcrypt（cost=12），单次约 250ms × 8 = 2s。
- **修复建议**：backup codes 改 SHA-256 或 Argon2id 低参数（一次性码、低价值，无需 bcrypt）。

#### - [x] P1-C10　RunPendingTasks 串行执行

- **位置**：`internal/logic/task/task.go:276-280`
- **问题描述**：for 循环顺序执行所有 pending 任务，单个长任务阻塞后续全部。
- **修复建议**：有界 goroutine 池并发执行，或按 handler 类型分批。

---

### 🌐 P1-Resource　资源/SSRF 类

#### - [ ] P1-R1　rehostFromURL SSRF 风险

- **位置**：`internal/logic/task/sync_image_worker.go:756-782`
- **问题描述**：直接 GET 上游返回的 URL 下载图片，无内网地址过滤。
- **影响**：恶意/被劫持渠道返回 `http://169.254.169.254/latest/meta-data/` 即可让 worker 拉取内网元数据。
- **修复建议**：过滤 link-local（169.254.0.0/16）、私有 IP 段（10/172.16/192.168）、loopback；解析 DNS 后再校验。

#### - [ ] P1-R2　payment LockOrder 引用计数锁竞态

- **位置**：`internal/logic/payment/callback.go:30-59`
- **问题描述**：`sync.Map` + 手工引用计数，`UnlockOrder` 中 `rcm.mu.Unlock()` 之后才 `refCount--`，存在归零后误 Delete 的窗口。
- **修复建议**：用 sharded mutex 或 singleflight 替代手写引用计数；或直接依赖 DB 条件更新删掉进程内锁。


#### - [ ] P1-R4　OAuth HTTP 客户端无超时

- **位置**：`internal/logic/tenant/oauth.go:516,556,624`
- **问题描述**：用 `http.DefaultClient`（无超时），上游慢响应会挂死 goroutine。
- **修复建议**：`&http.Client{Timeout: 10 * time.Second}`。

#### - [ ] P1-R5　dispatchAlertNotifications goroutine 无上限

- **位置**：`internal/logic/monitor/alert_engine.go:114` + `alert_notify.go:142`
- **问题描述**：ctx 是 `gctx.New()`（无超时），告警风暴时可能产生数百个挂起 goroutine。
- **修复建议**：给 ctx 加 timeout 或限制并发数。

---

### 🔄 P1-Transaction　事务边界类

#### - [x] P1-T1　SetTenantGroups 删-插无事务

- **位置**：`internal/logic/admin/model_group.go:323-349`
- **问题描述**：先 `Delete()` 旧关联，再 `Batch().Insert()` 新关联，两步不在同一事务。
- **影响**：插入失败（DB 抖动/唯一键冲突）会清空租户模型分组，影响下游定价。
- **修复建议**：包 `g.DB().Transaction`，参照项目正确写法 `tenant/member_model_scope.go:88`。
　**〔已修复〕** `SetTenantGroups` 的 Delete+BatchInsert 包进 `g.DB().Transaction`（ctx 传播式，闭包内统一 `dao.Xxx.Ctx(ctx)`）。同文件 `SetGroupModels` 存在相同删-插分离 bug，一并包进事务。

#### - [x] P1-T2　ApiKeyUpdate 主表与 scope 更新分离

- **位置**：`internal/logic/tenant/api_key.go:457-463`
- **问题描述**：Key 主表更新与 `updateApiKeyModelScopes` 两步分离，前者成功后者失败留中间态。
- **修复建议**：外层事务包住两步。
　**〔已修复〕** ApiKeyUpdate 主表 `Update` 与 `updateApiKeyModelScopes` 用外层 `g.DB().Transaction` 包住；后者在携带事务的 ctx 下被 GoFrame 视为嵌套事务（savepoint），与主表更新共享同一事务，任一失败整体回滚。`relay.InvalidateApiKey` 缓存失效移到事务提交后执行。

#### - [x] P1-T3　InvalidateWallet 在事务提交前调用

- **位置**：`internal/logic/payment/fulfill.go:207`
- **问题描述**：事务闭包内调 `billing.InvalidateWallet`（Redis DEL），但事务尚未提交。并发 `GetWallet` 可能从 DB 读到旧值回写 Redis。
- **修复建议**：把 `InvalidateWallet` 移到事务外（`FulfillOrder` 返回后调用）。
　**〔已修复〕** 移除 `creditWalletTx` 内的 `billing.InvalidateWallet`；`FulfillOrder` 改为先记录 `walletTouched` 标记，事务提交成功后再调 `billing.InvalidateWallet`（与 `redemption.go:152` 的「事务提交后清缓存」模式一致）。

#### - [x] P1-T4　handleDeletionRequest 跨表无事务

- **位置**：`internal/logic/task/data_tasks.go:111-155`
- **问题描述**：跨 `tnt_users`、`api_keys`、`aud_sensitive_access_logs`、`tnt_tenants` 4 张表删除/更新无事务。
- **影响**：中途失败留下部分应用的中间态（如 API Key 已禁用但租户未标记 terminated）。
- **修复建议**：`g.DB().Transaction` 包裹。
　**〔已修复〕** 4 张表的匿名化/禁用/删除/终止包进 `g.DB().Transaction`，任一步失败整体回滚，杜绝部分应用的中间态。

#### - [x] P1-T5　subscribePlanTx 先取消当前订阅无并发保护

- **位置**：`internal/logic/payment/fulfill.go:120-130`
- **问题描述**：把当前 active 订阅置 expired 再 INSERT 新的，两个并发履约可能都读到 active → 两条 active 订阅。
- **修复建议**：INSERT 后用唯一约束（tenant_id + status='active'）防重，或事务内 advisory lock。
　**〔已修复〕** 采用事务内 advisory lock：`subscribePlanTx` 入口 `SELECT pg_advisory_xact_lock(NS+tenantID)`（`planSubscribeLockNS` 高位命名空间段，避免与其他模块冲突）。事务级锁随提交/回滚自动释放，不占用行锁无死锁风险；后到事务阻塞至前者提交（旧 active 已 expired）后进入临界区，串行化同租户订阅。

---

## 🟡 P2 — 一般问题（可维护性 / 性能）

### - [x] P2-3　N+1 查询

- **位置**：`internal/logic/admin/model_import_export.go:79-126`
- **问题描述**：ExportModelsJson 循环每个模型查 `dao.MdlPricing`。
- **修复建议**：参照 `ListModels`（`model.go:128-136`）用 `WhereIn("model_id", modelIDs)` 一次拉齐。
　**〔已修复〕** 改为单次 `WhereIn("model_id", modelIDs)` 批量查询全部定价行，按 `model_id` 分组组装，与 `ListModels` 的批量模式对齐。

### - [x] P2-4　错误吞掉（仪表盘静默返回 0）

- **位置**：`internal/logic/admin/dashboard.go:29-39,46-87`
- **问题描述**：7 个聚合 Count 全部 `_, _ =` 吞错误，仅 Warning 日志。
- **修复建议**：关键统计返回 error 让上层降级，至少不要 `_` 吞掉。
　**〔已修复〕** 三处 `_, _ =` Count 改为捕获 err 并 `g.Log().Warningf` 记录（与下方 scan 查询一致的降级风格），不再静默吞错。

### - [x] P2-5　map[string]any 返回值（类型不安全）

- **位置**：`tenant/promo_code.go:81-154`、`admin/notification.go:198-249`、`admin/audit.go` 多处
- **问题描述**：内部函数返回 map，调用方靠 `result["x"].(int64)` 类型断言。
- **修复建议**：改 struct。
　**〔已修复〕** `validatePromoCodeInternal` 返回新增的 `promoValidationResult` struct（取代 `g.Map` + 4 处类型断言）；`GetMessageReadStats` 返回新增的 `MessageReadStats` struct（取代 `map[string]any`）。`admin/audit.go` 的 `map[string]any` 是动态列（DB 行直透到 `List []map[string]any` 响应），非类型断言用法，不在本次范围。

### - [x] P2-6　CleanExpiredPreDeducts 无分布式锁

- **位置**：`internal/logic/billing/reconciliation.go:167-231`
- **问题描述**：多实例部署时 N 个实例同时处理同一批 expired tracks，钱包 UPDATE 无 status 守卫。
- **修复建议**：钱包 UPDATE 加 `AND EXISTS (...)` 谓词，或 Redis 分布式锁包裹。
　**〔已修复〕** 改为「先 claim 再 release」：先用带 `status='frozen'` 谓词的条件 UPDATE 把每条 track 标记 expired，仅对 `RowsAffected > 0`（本实例真正认领到）的 track 累加金额，再释放恰好这部分金额。`status='frozen'` 的条件更新是跨实例原子闸门，各实例释放金额之和恒等于实际冻结总额，无重复/超额释放。

### - [x] P2-7　trackPreDeduct 异步写失败无补偿

- **位置**：`internal/logic/billing/wallet.go:482-491`
- **问题描述**：异步 INSERT `bil_prededuct_tracks` 失败仅 Warning，Redis 重启后 `rebuildPredeductFromDB` 无法恢复该预扣。
- **修复建议**：失败重试或写失败队列。
　**〔已被 P1-C1/C3 修复覆盖〕** 当前 `trackPreDeduct` 已是**同步**写入（`wallet.go:201-207`、`237-242`），且失败时主动回滚 DB 冻结（`unfreezeDBAmount`）与 Redis 预扣（`rollbackRedisPreDeduct`）后再上抛错误。原「异步 + 仅 Warning」模式不再存在，无需额外补偿队列。

### - [x] P2-8　ProcessCallback 金额校验容差用 float64

- **位置**：`internal/logic/payment/callback.go:108-119`
- **问题描述**：容差 0.01 CNY 用 float64 减法。
- **修复建议**：用 decimal 比较；容差按比例或更小绝对值。
　**〔已修复〕** 容差比较改为 `decimal`：`paid.Sub(expected).Abs().GreaterThan(paymentAmountTolerance)`，`paymentAmountTolerance = decimal.NewFromFloat(0.01)`，消除 0.01 边界值的二进制误差。

### - [x] P2-9　GetEnabledChannels 无缓存

- **位置**：`internal/logic/payment/config.go:114-159`
- **问题描述**：每次调用都查 `sys_options`。
- **修复建议**：加 60s 内存缓存。
　**〔已修复〕** 新增进程内 60s 结果缓存 `enabledChannelsCache`（`common.NewCache` + `GetOrSet` singleflight），计算逻辑抽到 `loadEnabledChannels`；`SaveChannelConfig` 写入成功后主动 `Delete` 失效缓存，保证配置变更即时生效。

### - [x] P2-10　tenant_auth OAuth callback 路径前缀过宽

- **位置**：`internal/middleware/tenant_auth.go:55-58`
- **问题描述**：任何 `/api/tenant/oauth/{任意串}/callback` 都免认证。
- **修复建议**：`provider` 限定已知集合（`openai|claude|gemini`）。
　**〔已修复〕** 新增 `isOAuthCallbackPath` + `allowedOAuthProviders` 白名单（`github`/`google`，与 `tenant/oauth.go` providers 注册表一致），仅已知 provider 的单段路径放行，其余回退到正常 JWT 鉴权。

### - [x] P2-12　evaluateCondition eq 用 == 比较 float64

- **位置**：`internal/logic/monitor/alert_engine.go:146`
- **修复建议**：`math.Abs(value-threshold) < epsilon`，或文档化 eq 仅适用整数指标。
　**〔已修复〕** `eq` 改为 `math.Abs(value-threshold) < alertFloatEpsilon`（`alertFloatEpsilon = 1e-9`），吸收二进制表示误差。

### - [x] P2-13　api_metrics 无 tenant 隔离

- **位置**：`internal/logic/monitor/api_metrics.go:53-56` 等
- **问题描述**：所有查询未带 tenant_id。
- **修复建议**：管理后台用合理（平台视角），但若复用到租户接口会泄露数据，加调用方校验。
　**〔已修复〕** 新增 `requireAdminScope(ctx)` 调用方域校验（admin/内部放行，tenant/未知拒绝），并在暴露跨租户聚合的 sMonitor 入口（`Dashboard`/`Traffic`/`Latency`）调用；同时在 `api_metrics.go` 顶部补平台域只读约定注释。

### - [x] P2-14　api_key listProjectApiKeys 疑似死代码

- **位置**：`internal/logic/tenant/api_key.go:588-657`
- **问题描述**：与 `ApiKeyList` 重复，且 grep 仅定义处命中，未被调用。
- **修复建议**：确认后删除。
　**〔已修复〕** 确认全仓 grep 仅定义处命中（含 service/controller），无任何调用方，已删除。

---

## 🔵 P3 — 细节优化

- [x] **P3-1** `tenant/auth.go:362-366` 锁定剩余分钟用 `int(remaining)` 截断，"还剩 0.9 分钟"显示"0 分钟"。改 `int(math.Ceil(remaining))`。
- [x] **P3-2** `tenant/member.go:866` 找不到成员返回 `NewBadRequestError`，其它方法返回 `NewNotFoundError`，语义不一致。统一。
- [x] **P3-3** `tenant/auth.go:498` 用 `time.RFC3339`，`oauth.go:328` 硬编码等价格式，风格不一。统一。
- [x] **P3-4** `consts/consts.go` 错误码分段有空洞（10067、10070-72、10099+）且 10403+ 跳跃编号。统一分段策略或加注释。
- [x] **P3-5** `consts.go` `MsgSuccess = "OK"` 与 `response.go:25` 硬编码 `"ok"` 大小写不一。统一。
- [x] **P3-6** `middleware/request_id.go:11` 信任客户端 `X-Request-Id` 未校验格式。校验 `^[a-zA-Z0-9-]{1,64}$`。
- [x] **P3-7** `admin/auth.go:317-329` LIKE 搜索 `%`/`_` 通配符未转义。转义或用全文索引。
- [x] **P3-8** `utility/crypto.go:21` `Encrypt` 返回 base64 无版本前缀。加 `v1:` 前缀便于密钥轮换。
- [x] **P3-9** `utility/turnstile.go:53` `io.ReadAll` 无大小限制。改 `io.LimitReader(resp.Body, 64*1024)`。
- [x] **P3-10** `utility/totp.go:25` TOTP 用 SHA-1，新部署建议默认 SHA-256。　**〔已核实不可直接切换〕** `totp.Validate` 硬编码 SHA-1 且存储链路只持久化 base32 secret（不保存算法元信息），生成改 SHA-256 会导致服务端校验恒失败。已保留 SHA-1 并在源码注释写明约束与未来切换所需的前置条件（持久化 per-secret 算法 + 改用 `ValidateCustom`），属涉及 schema 与 2FA 安全路径的较大改动，超出 P3 范围。
- [x] **P3-11** `utility/totp.go:43` backup codes 48-bit 熵偏低，提升到 64-bit 零成本。
- [x] **P3-12** `billing/level.go:178` `hasUpdate := true` 初始即 true 使判断失效。改 false 根据变化置 true。
- [x] **P3-13** `billing/pricing.go:242` `ModelMultiplier` 硬编码 1.0 从未被配置覆盖，要么实现真实来源要么删除。　**〔已文档化〕** 该字段是计费公式 `基础价 × 模型乘数 × 租户乘数` 的设计位 + `bil_records.model_multiplier` 快照列，删除会破坏快照结构；当前 `mdl_models` 无 multiplier 字段故恒为 1.0，已加注释说明占位语义与未来启用路径。
- [x] **P3-14** `task/lifecycle_cron.go:42-46` `if count > 0 || true` 恒真表达式，调试残留。
- [x] **P3-15** `handler/relay/relay.go:500-507` recordAudit 对 multipart 体强行 Unmarshal 且写入完整二进制。跳过或仅记元数据。
- [x] **P3-16** `handler/relay/relay.go:688-708` HandleRealtime 缺监控埋点和 panic recover。
- [x] **P3-17** `internal/response/response.go:72` error log 全量读 body，multipart 会写 base64 图片。跳过 multipart。
- [x] **P3-18** `billing/summary.go` BuildBillingSummary 与 `snapshot.go` GenerateBillingSummary 功能重叠。统一。　**〔已删冗余〕** 经查 `BuildBillingSummary`/`SummaryParams` 仅被 `summary_test.go` 引用，生产链路统一走 `GenerateBillingSummary`；已删除死代码与对应测试，保留共用的 `formatInt`/`formatCost`。
- [x] **P3-19** `cmd.go:119-193` cron 注册散落 15 处，抽 `registerCronJobs(cs)`。
- [x] **P3-20** `open_api.go:29` 与 `provider.go:1156` cache name 同为 `"member_model"`，可能互相覆盖。改 `"open_member_model"`。　**〔已核实为有意共享 + 重构〕** 三处同前缀是故意的跨包失效机制：`relay` 是生产者（Get/Set），`tenant`/`open` 仅 Delete 失效。原建议的「改名」会破坏失效。改为抽取 `relay.InvalidateMemberModelScopeCache` 单一失效入口，消除重复字面量与被误判为命名冲突的隐患。

---

> 修复一条后将对应 `- [ ]` 改为 `- [x]`，并更新本表数字。

---

*文档结束。如需针对某一条给出具体修复 diff，告知条目编号即可。*
