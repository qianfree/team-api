{
  "model": "origin-model",
  "messages": [
    {"role": "user", "content": "Search the repo with the where filter."}
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "grep",
        "description": "search repo",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {
              "type": "object",
              "properties": {
                "pattern": {"type": "string"},
                "where": {
                  "type": "array",
                  "maxItems": 10,
                  "items": {
                    "type": "array",
                    "prefixItems": [
                      {"type": "string"},
                      {"enum": ["AND", "OR"], "type": "string"},
                      {}
                    ]
                  }
                },
                "paths": {"type": "array"},
                "tag": {"anyOf": [{"type": "string"}, {"type": "number"}]}
              },
              "required": ["pattern"]
            }
          },
          "required": ["query"]
        }
      }
    }
  ],
  "max_tokens": 512
}
