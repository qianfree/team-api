# feat/responses 分支待办事项

> 来源：2026-08-17 对照《docs/大模型协议Usage字段调研.md》的 token 处理审计。
> 已完成：P0（Gemini thoughts 计费）、P1（Claude→OpenAI 加法口径 + bridge 联动）、
> P3（input_tokens 口径统一 + 000017 回填迁移）、P4（cache_write_tokens 解析与可见）、
> P5（插件 TokenUsage 扩展）。以下为**未处理**事项。

---

## A. 已确认未修的代码问题

### 1. Claude 5m/1h 缓存写入未分别计价（原 P2，收入项）⚠️ 高

- **位置**：`internal/logic/billing/pricing.go` `computeCost` / `resolveTokenCounts`
- **问题**：全部 `cacheCreationTokens` 按单一 `CacheCreationPrice` 计价；`CachedCreation5mTokens/1hTokens` 仅入库不参与计价。官方倍率：5m=1.25×、1h=2×，老响应无 TTL 细分时默认按 5m。
- **影响**：`CacheCreationPrice` 按 5m 配置则 1h 写入低估约 37.5%；反之 5m 高估。1h 缓存用量越大偏差越大。
- **修复思路**：
  1. `PricingResult` 新增 1h 价格字段（或 1h 倍率），需管理后台配置入口
  2. `resolveTokenCounts` 分桶返回 5m/1h
  3. `computeCost` 分别计价；无 TTL 细分时全部按 5m 兜底
- **前置**：需先决策定价配置方案（独立字段 vs 倍率）

### 2. OpenAI→Gemini 反向转换 reasoning 双计（小修）✅ 建议顺手修

- **位置**：`relay/channel/openai/gemini_response.go:177,225`
- **问题**：`CandidatesTokenCount = usage.CompletionTokens`——OpenAI 的 completion **含** reasoning，而 Gemini 语义 candidates **不含**思考，且同处又单独设了 `ThoughtsTokenCount`。Gemini 客户端按 `total = prompt + candidates + thoughts` 自行汇总时 reasoning 被双计。
- **修复**：`CandidatesTokenCount: completion - reasoning`（判空 details）
- **影响**：仅 Gemini 格式客户端展示，不影响计费

### 3. Claude 服务端工具调用未计费（收入项）⚠️ 高

- **位置**：`relaykit/dto/claude.go` `ClaudeServerToolUsage` 已解析，计费链路（`internal/logic/billing/`）完全忽略
- **问题**：Claude `usage.server_tool_use.web_search_requests / web_fetch_requests` 按次计费（如 web search $10/1k 次），当前漏收。
- **修复思路**：`TokenDetails` 增加按次计数字段 → usage log 落库 → `computeCost` 增加按次项；需模型定价新增服务端工具单价配置

### 4. 流中断请求的用量报表缺口

- **位置**：`relay/handler/relay_handler.go` 流中断路径（`SettleStreamInterrupted` → `recordFailedUsage`）
- **问题**：计费按 `ApplyInterruptedUsageFallback` 估算实际收了钱，但 `recordFailedUsage` 写的 usage log **token 列全为 0**——计费与报表脱节，token 统计系统性偏低。
- **修复思路**：中断路径将估算 usage 传入 `recordFailedUsage`（或改用带 token 的记录路径）
- **建议**：与 #1 一起做，同动结算/报表链路

---

## B. 部署顺序注意事项（非代码 bug）

### 5. 000017 迁移执行时序 ⚠️ 必须遵守

回填 UPDATE 会给所有 `channel_type=2 AND cache>0` 的行加缓存。若新代码先上线、迁移后执行，窗口期新写入的行（已含缓存口径）会被**二次加 cache 双计**。

正确顺序：**停旧服务 → `goose up`（000017）→ `team-api aggregate-usage --from <最早用量日期> --to <今天>` → 启新服务**

### 6. 部署当天 Redis 热桶混合口径

模型性能页"今天"数据（热桶）当天混合新旧口径，次日 01:00 聚合进 `bil_usage_daily` 后自动消解，无需处理。

---

## C. 待确认 / 未审计项

| # | 项 | 说明 |
|---|---|---|
| 7 | Gemini `toolUsePromptTokenCount` | 官方口径模糊（是否含于 promptTokenCount 不明），当前忽略，需查证后决定是否计费 |
| 8 | Gemini 渠道 → Claude 格式客户端 | relaykit 注册了 `gemini→claude` 转换器但 bridge 未接该方向；该路径是否可达、usage 口径是否正确未验证 |
| 9 | Vertex Code Assist 聚合流 | `handleCodeAssistAggregatedStream` 的 usage 处理未验证 |
| 10 | 音频 token 计费 | `audio_input/output_tokens` 已入库，是否有独立单价、计费是否正确未审计 |
| 11 | 插件 `RelayInterceptor` 接线 | P5 只扩展了 `TokenUsage` 结构；填充点（`RelayResponse.Usage` 构造）待插件系统接入 relay 流程时实现 |
| 12 | playground `calculateCost.ts` 无缓存价 | P1 后 prompt 已含缓存，按全价估算偏高；仅影响演示页展示 |

---

## 建议处理顺序

1. **#2**（一处减法，最小成本）
2. **#1 + #4**（收入项 + 同链路报表，需先定 #1 定价配置方案）
3. **#3**（收入项，需新增定价配置）
4. C 组按需排查
