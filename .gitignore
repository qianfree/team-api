# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# Node.js
node_modules/
dist/
build/

# IDE
.idea/

# Environment files with sensitive data
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp

# 外部文件
new-api/
sub2api/

# 配置文件（含敏感信息，使用 .example 模板代替）
hack/config.yaml
manifest/config/config.yaml


# 防误写保险：语料默认输出在仓库外（../team-api-corpus / TEAM_API_CORPUS_DIR），
# 万一有人 -o 指回仓库内也不至于误提交。见 docs/协议转换语料采集手册.md §4
relaykit/relayconvert/register/golden/real_inputs/
relaykit/relayconvert/register/golden/real_stream_inputs/
relaykit/relayconvert/register/golden/real_responses/
relaykit/relayconvert/register/golden/corpus_manifest.json
